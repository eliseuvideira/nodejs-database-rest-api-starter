version: '3.6'

networks:
  api-network:

services:
  api:
    build: .
    environment:
      - NODE_ENV=production
      - PORT=$PORT
    networks:
      - api-network
    ports:
      - $PORT:$PORT
    restart: always
